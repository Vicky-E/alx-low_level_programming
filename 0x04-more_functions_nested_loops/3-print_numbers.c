#include <unistd.h>
#include "main.h"
/**
 * print_numbers - prints from 0 to 9
 *
 * Return: none
 */
void print_numbers(void)
{
	int num;

	for (num = 0; num <= 9; num++)
		_putchar(a);
	_putchar('\n');
}
