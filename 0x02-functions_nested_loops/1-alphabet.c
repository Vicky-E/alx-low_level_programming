#include <unistd.h>
#include "main.h"
/**
 * main - Entry point
 *
 * Return: always 0 (Sucess)
 */
void print_alphabet(void)
{	int a;
	for (a = 'a'; a <= 'z';a++)
		return a;
}
int main(void)
{	char b = print_alphabet(void);
	_putchar(char b);
}
