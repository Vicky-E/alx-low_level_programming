#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{char x;
for (x = 'a'; x <= 'z'; x++){
	putchar(x);
	if (x = q || x = e){
		continue;
	}
putchar('\n');
return (0);
}
