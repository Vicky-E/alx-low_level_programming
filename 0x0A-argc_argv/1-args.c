#include <stdlib.h>
#include <stdio.h>
/**
 * main - prints argument count
 * @argc: count
 * @argv: vector
 * Return: 0 on sucess
 */
int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
	{
		argv[i];
	}
	printf("%d\n", argc);
	return (0);
}
