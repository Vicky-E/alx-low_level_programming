#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints name of program
 * @argc: argument count
 * @argv: array of arguments
 * Return: 0 on sucess
 */
int main(__attribute__((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
