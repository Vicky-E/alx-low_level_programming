#include <unistd.h>
#include "main.h"
/**
 * mul - multiplies integers
 * @a: integer to be multiplied
 * @b: integer to be multiplied
 *
 * Return: x.
 */
int mul(int a, int b)
{
	int x;

	x = a * b;

	return (x);
}
