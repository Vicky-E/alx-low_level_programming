#include <stdio.h>
/**
 * main - Entry point
 *
 * Return:Always 0 (Sucess)
 */
int main (void)
{ char first[] = "\"Programming is like building a multilingual puzzle";
  puts(first);
	return (0);
}

